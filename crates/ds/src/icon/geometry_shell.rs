//! Lucide geometry for the shell set (design/08-ICONS.md section 1.6), one constant per glyph,
//! transcribed from lucide.dev at the pinned version. ISC licence; see `super`.
#![allow(unused_variables)] // Freeze stubs: remove with the last todo!().

use super::Icon;
use super::shape::Shape;

/// The children of a shell-set glyph. The mailo set never reaches here.
pub(super) fn shapes(icon: Icon) -> &'static [Shape] {
    todo!()
}
