# The whole program (quire, shell-host, sill) builds on one toolchain. blitz at the pinned rev
# needs 1.91; 1.98.1 is what this machine has installed. Raised only in a toolchain-bump wave.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
